# Keyword Explorer — v1.1.0

> First ASO (App Store Optimization) feature. Analyzes keyword density in app descriptions and enables cross-app comparison.

## Current State

- **Backend:** Ready — `KeywordController` exists (`keywords`, `keywords/compare` endpoints)
- **Frontend:** Missing — shows "Coming Soon" in sidebar
- **MCP:** Missing — no keyword tools yet

## Tasks

### 1. Web UI — Keyword Explorer Page

**File:** `web/src/pages/keywords/Explorer.tsx`

- App selector (from tracked app list)
- Keyword density table (word, count, density %)
- Two-app comparison view (side by side)
- Platform and language filter

### 2. Router & Sidebar

- `web/src/router.tsx` → add `/keywords/explorer` route
- `web/src/layouts/AppLayout.tsx` → remove `comingSoon: true` from `asoItems`, update `href`

### 3. API Client

- Run `make api` → keyword endpoints auto-generated via Orval

### 4. MCP Tools

- `mcp/src/tools/keywords.ts` → `get_keyword_density`, `compare_keywords` tools
- `mcp/src/register.ts` → register new tools
- npm version bump + publish

### Verification

```bash
make lint && make test
# /keywords/explorer page loads
# Keyword density table renders
# Two-app comparison works
```
