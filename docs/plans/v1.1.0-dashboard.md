# Dashboard Page — v1.1.0

> Post-login home page. Summarizes tracked app count, recent changes, recent reviews, and overall status.

## Current State

- **Backend:** Ready — `DashboardController` exists, `/api/v1/dashboard` endpoint works
- **Frontend:** Missing — users are redirected to `/discovery/trending` after login

## Tasks

### 1. Web UI — Dashboard Page

**File:** `web/src/pages/Dashboard.tsx`

Cards:
- Tracked app count (iOS / Android split)
- Number of apps changed in last 7 days
- Recent reviews (mini list)
- Recent changes (mini list)
- Quick actions (add app, go to trending)

### 2. Router

- `web/src/router.tsx` → add `/dashboard` route
- Change post-login redirect from `/discovery/trending` to `/dashboard` (in `GuestGuard` and `AuthGuard`)

### 3. Sidebar

- `web/src/layouts/AppLayout.tsx` → add "Dashboard" link above Discovery group

### 4. API Client

- Run `make api` → verify dashboard endpoint is generated

### Verification

```bash
make lint && make test
# Login → auto-redirects to /dashboard
# Cards show correct data
# Empty state (0 apps) renders cleanly
```
